generator client {
  provider   = "prisma-client-js"
  engineType = "library"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                 String           @id @default(cuid())
  name               String
  email              String           @unique
  passwordHash       String
  roles              Role[]           @default([DESENVOLVEDOR])
  avatarUrl          String?
  mustChangePassword Boolean          @default(true)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  assignments        TaskAssignment[]
}

model TaskAssignment {
  id           String   @id @default(cuid())
  taskId       String   @unique // ID do Taskrow (ex: TR-216967)
  title        String
  client       String
  userId       String // ID do User local
  status       String   @default("ASSIGNED")
  column       String   @default("todo") // todo, inProgress, inReview, done
  estimationHr String?
  qaApproved   Boolean  @default(false)
  user         User     @relation(fields: [userId], references: [id])
  createdAt    DateTime @default(now())
}

model GlobalSettings {
  id                 String   @id @default("global")
  hiddenSidebarItems String[] @default([]) // Guarda as rotas ocultas
  updatedAt          DateTime @updatedAt
}

enum Role {
  GESTOR
  ADMINISTRADOR
  DESENVOLVEDOR
}
